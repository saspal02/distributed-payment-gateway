apiVersion: apps/v1
kind: Deployment
metadata:
  name: vault-service
  namespace: paygrid-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vault-service
  template:
    metadata:
      labels:
        app: vault-service
    spec:
      containers:
        - name: vault-service
          image: docker.io/saspal02/paygrid-vault-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9040
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: VAULT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: VAULT_DB_PASSWORD
            - name: VAULT_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: VAULT_MASTER_KEY
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REDIS_PASSWORD
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9040
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: vault-service
  namespace: paygrid-core
spec:
  selector:
    app: vault-service
  ports:
    - port: 80
      targetPort: 9040
